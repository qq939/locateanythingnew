# 2026-06-05

- Fixed LocateAnything environment handling after `No module named 'torch'`.
- Confirmed `.venv/bin/python` has `torch 2.12.0`, `transformers 5.10.1`, `Pillow 12.2.0`, and `huggingface_hub 1.17.0`.
- Updated `server.js` to prefer project `.venv/bin/python` even when started directly with `node server.js`.
